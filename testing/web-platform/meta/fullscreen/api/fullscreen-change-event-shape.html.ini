[fullscreen-change-event-shape.html]
  [run-the-fullscreen-steps: pending events list is drained per invocation (no duplicate events)]
    expected:
      if (os == "linux") and not debug: [PASS, FAIL]
