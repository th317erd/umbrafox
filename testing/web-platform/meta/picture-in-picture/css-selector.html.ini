[css-selector.html]
  expected:
    if (os == "win") and not debug and (processor == "x86"): [OK, TIMEOUT]
    if (os == "mac") and not debug and (processor == "x86_64"): [TIMEOUT, OK]
    if (os == "mac") and not debug and (processor == "aarch64"): [OK, TIMEOUT]
    if (os == "mac") and debug: [TIMEOUT, OK]
  max-asserts: 2
  [Entering and leaving Picture-in-Picture toggles CSS selector]
    expected:
      if (os == "win") and not debug and (processor == "x86"): [PASS, TIMEOUT]
      if (os == "mac") and not debug and (processor == "x86_64"): [TIMEOUT, PASS]
      if (os == "mac") and not debug and (processor == "aarch64"): [PASS, TIMEOUT]
      if (os == "mac") and debug: [TIMEOUT, PASS]
