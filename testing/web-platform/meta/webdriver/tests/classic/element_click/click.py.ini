[click.py]
  expected:
    if (os == "linux") and (display == "wayland") and debug: [OK, TIMEOUT, CRASH]
    if (os == "linux") and (display == "wayland") and not debug: [OK, TIMEOUT]
  [test_null_response_value]
    expected:
      if (os == "linux") and (display == "wayland") and debug: [PASS, FAIL]
