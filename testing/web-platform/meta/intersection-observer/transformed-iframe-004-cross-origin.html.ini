[transformed-iframe-004-cross-origin.html]
  [Target is not visible as it's rotated perpendicularly to viewport]
    expected:
      if (os == "linux") and fission and asan: [PASS, FAIL]
      if (os == "mac") and debug: [PASS, FAIL]
      if (os == "linux") and not fission: FAIL
      if os == "android": FAIL
