[encodedVideoChunk-serialization.crossAgentCluster.https.html]
  expected:
    if (os == "linux") and (display == "wayland") and debug: [OK, TIMEOUT]
    if (os == "linux") and (display == "wayland") and not debug: [OK, TIMEOUT]
  [Verify chunks cannot be passed to serviceworker]
    expected:
      if (os == "linux") and (display == "wayland") and debug: [PASS, TIMEOUT]
      if (os == "linux") and (display == "wayland") and not debug: [PASS, TIMEOUT]

  [Verify chunks is unavailable in sharedworker]
    expected:
      if (os == "linux") and (display == "wayland") and debug: [PASS, NOTRUN]
      if (os == "linux") and (display == "wayland") and not debug: [PASS, NOTRUN]

  [Verify chunks is unavailable in serviceworker]
    expected:
      if (os == "linux") and (display == "wayland") and debug: [PASS, NOTRUN]
      if (os == "linux") and (display == "wayland") and not debug: [PASS, NOTRUN]
