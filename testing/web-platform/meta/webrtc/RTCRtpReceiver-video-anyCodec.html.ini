[RTCRtpReceiver-video-anyCodec.html]
  expected:
    if (os == "android") and not debug: [OK, TIMEOUT]
  [An RTCRtpReceiver is prepared to receive any negotiated codec.]
    bug: https://bugzilla.mozilla.org/show_bug.cgi?id=1952881
    expected:
      if (os == "android") and not isolated_process and not debug: [PASS, TIMEOUT, FAIL]
      if (os == "android") and isolated_process: [FAIL, PASS]
      if os == "win": [PASS, FAIL, PRECONDITION_FAILED]
      [PASS, FAIL]
