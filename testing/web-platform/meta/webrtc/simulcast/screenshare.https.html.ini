[screenshare.https.html]
  disabled:
    if os == "android": no screen capture on android
  expected:
    if (os == "mac") and debug: [OK, ERROR]
    if (os == "mac") and not debug: [TIMEOUT, OK, ERROR]
  [Basic simulcast setup with two spatial layers]
    bug: https://bugzilla.mozilla.org/show_bug.cgi?id=2057254
    expected:
      if (os == "mac") and debug: [PASS, FAIL]
      if (os == "mac") and not debug: [TIMEOUT, PASS, FAIL]
