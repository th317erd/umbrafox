[window-open-defaults.window.html]
  expected:
    if (os == "linux") and debug and not fission: [OK, TIMEOUT]
    if (os == "win") and debug: [TIMEOUT, OK]
    if (os == "win") and not debug: [OK, TIMEOUT]
    if (os == "linux") and not debug: [OK, TIMEOUT]
  [window.open()'s url parameter default]
    expected:
      if (os == "linux") and fission and not asan: [PASS, TIMEOUT]
      if (os == "win") and debug: TIMEOUT
      if (os == "win") and not debug: [PASS, TIMEOUT]
