[window-open-invalid-url.html]
  expected:
    if (os == "linux") and not asan and fission and tsan: [OK, TIMEOUT]
    if (os == "win") and (processor == "x86") and debug: [OK, ERROR, TIMEOUT, CRASH]
    if (os == "win") and (processor == "x86") and not debug: [OK, ERROR, TIMEOUT, CRASH]
    if (os == "linux") and asan and not fission: [OK, TIMEOUT]
    if (os == "linux") and not asan and not fission: [OK, TIMEOUT]
    if (os == "win") and (processor == "x86_64"): [OK, TIMEOUT]
