[observer-in-cross-origin-frame.sub.html]
  [Cross-origin observer responds to explicitly set physical size]
    expected:
      if not tsan and fission and not asan and debug and (os == "win") and (processor == "x86_64"): [PASS, FAIL]
      if not tsan and fission and not asan and debug and (os == "mac"): [PASS, FAIL]
      if not tsan and not fission and (os == "android") and not debug: [PASS, FAIL]
      if not tsan and fission and asan: [PASS, FAIL]
      if tsan: [PASS, FAIL]

  [When size is viewport-dependant, cross-origin observer responds to viewport size changes]
    expected:
      if (os == "linux") and fission and not asan and not debug and tsan: [PASS, FAIL]
      if (os == "linux") and fission and not asan and debug: [PASS, FAIL]
      if (os == "win") and debug and (processor == "x86_64"): [PASS, FAIL]
      if (os == "win") and debug and (processor == "x86"): [PASS, FAIL]
      if (os == "win") and not debug and (processor == "x86"): [PASS, FAIL]
      if (os == "linux") and fission and asan: [FAIL, PASS]
