[drm-mp4-playback-temporary-playduration-keystatus.https.html]
  disabled:
    if os == "mac": https://bugzilla.mozilla.org/show_bug.cgi?id=2067309
  expected:
    if os == "win": TIMEOUT
  [drm, temporary, mp4, playback with limited playduration, check keystatus, single key]
    expected:
      if os == "win": TIMEOUT
