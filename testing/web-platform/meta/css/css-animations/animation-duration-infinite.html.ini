[animation-duration-infinite.html]
  expected:
    if debug and (os == "linux") and not fission and swgl: [PASS, ERROR]
    if debug and (os == "linux") and not fission and not swgl: [PASS, ERROR]
    if debug and (os == "linux") and fission: [ERROR, PASS]
    if debug and (os == "win"): [ERROR, PASS]
    if debug and (os == "android"): ERROR
