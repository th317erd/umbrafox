[getBoundingClientRect-svg.html]
  expected:
    if (os == "android") and fission: [TIMEOUT, OK]
  [Element.getBoundingClientRect() and Range.getBoundingClientRect() should match for an SVG <text> with a rotate]
    expected: FAIL
