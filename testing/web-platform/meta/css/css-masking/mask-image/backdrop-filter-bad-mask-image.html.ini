[backdrop-filter-bad-mask-image.html]
  # The about:blank reference is captured as an all-zero buffer when the
  # compositor snapshot races browser startup, see bug 2072328.
  expected:
    if asan and not fission and not swgl: TIMEOUT
    if tsan: [PASS, FAIL]
