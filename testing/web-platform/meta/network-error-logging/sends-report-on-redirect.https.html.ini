[sends-report-on-redirect.https.html]
  expected:
    if (os == "mac") and (version == "OS X 14.7.5"): [TIMEOUT, OK]
    if (os == "linux") and not debug: OK
    if os == "win": OK
    [OK, TIMEOUT]
  [\n    Test that NEL reports are sent for redirects\n  ]
    expected:
      if (os == "mac") and (version == "OS X 14.7.5"): [TIMEOUT, PASS]
      if (os == "linux") and not debug: [PASS, FAIL]
      if os == "win": PASS
      [PASS, TIMEOUT]
