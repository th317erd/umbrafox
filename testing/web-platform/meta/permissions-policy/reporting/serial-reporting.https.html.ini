[serial-reporting.https.html]
  prefs: [dom.webserial.testing.enabled:true,dom.webserial.gated:false]
  [requestPort in serial reporting mode]
    expected:
      if os == "android": FAIL

  [getPorts in serial reporting mode]
    expected:
      if os == "android": FAIL
