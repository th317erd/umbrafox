[cache-add.https.any.html]
  max-asserts: 2
  [Cache.addAll should succeed when entries differ by vary header]
    expected:
      if (os == "linux") and debug and fission and (display == "x11"): [PASS, FAIL]
      if (os == "linux") and debug and fission and (display == "wayland"): [PASS, FAIL]
      if (os == "android") and debug and isolated_process: [PASS, FAIL]
      if (os == "android") and not debug: [PASS, FAIL]
      [FAIL, PASS]


[cache-add.https.any.sharedworker.html]
  max-asserts: 2
  [Cache.addAll should succeed when entries differ by vary header]
    expected:
      if (os == "linux") and not debug and not asan: [FAIL, PASS]
      if (os == "win") and debug and (processor == "x86_64"): [PASS, FAIL]
      [FAIL, PASS]


[cache-add.https.any.serviceworker.html]
  disabled:
    if privateBrowsing: https://bugzilla.mozilla.org/show_bug.cgi?id=1320796
  [Cache.addAll should succeed when entries differ by vary header]
    expected: [FAIL, PASS]


[cache-add.https.any.worker.html]
  max-asserts: 2
  [Cache.addAll should succeed when entries differ by vary header]
    expected:
      if fission and (os == "linux") and not debug and (display == "wayland"): [PASS, FAIL]
      [FAIL, PASS]
