//error: redeclaration of symbol `f', first declared at

[ChildProc=any]
sync protocol PredeclParamReturn {

    // it's an error to name a parameter with the same id as a return

parent: async Msg(int f) returns (bool f);

};
