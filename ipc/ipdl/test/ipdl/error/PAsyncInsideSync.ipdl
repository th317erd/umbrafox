// inside_sync nested messages must be sync

//error: inside_sync nested messages must be sync (here, message `Msg' in protocol `PAsyncInsideSync')
//error: message `Msg' requires more powerful send semantics than its protocol `PAsyncInsideSync' provides

[ChildProc=any]
protocol PAsyncInsideSync {
child:
    [Nested=inside_sync] async Msg();
};
