[DEFAULT]
run-if = [
  "!opt && crashreporter", # the tests need the debug-only build ID mismatch hook
]

support-files = ["head.js"]
prefs = [
  "dom.ipc.processCount=1",
  "dom.ipc.processPrelaunch.fission.number=0",
]

["browser_aboutRestartRequired_basic.js"]
